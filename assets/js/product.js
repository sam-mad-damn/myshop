document.addEventListener("DOMContentLoaded", () => {
    // document.addEventListener("click", async (e) => {
    //     if (e.target.classList.contains("btn_basket")) {
    //         let id = e.target.dataset.productPositionId;
    //         await postJSON("/app/tables/baskets/work.basket.php", id, "add");
    //     }
    // })
})